L6:

Grid
StackPanel
WrapPanel
DockPanel
UniformGrid
Canvas
一般的布局控件继承Panel

Border是内容控件，作用也是布局
拖动窗体：空格键 + 鼠标拖动

Grid：行列，默认一行一列

Window下只能有一个控件，Contect属性只是一个对象。


L7:
嵌套

Auto：根据内容调整高度宽度

按比例分配：
Heigt = "2*"
Heigt = "1*"

合并：
Grid.ColumnSpan="2"


L8：
GridSplitter
ShowPreview属性

L9：
StackPanel
类似Div
堆的控件默认垂直摆放，修改Orientation属性

Margin属性：4个值分别代表左上右下


L10：
Board
属于装饰器
BorderThickness属性跟Margin一样，可以有4个值。
CornerRaduis属性

可以作为分割线，可以把Grid、StackPanel和按钮放里面，有圆角效果。
Border下只能有一个控件。当然也可以像Window控件那样嵌套布局控件。

L11：
用于分隔

L12:
WrapPanel
布局超出范围时，自动换行。（跟StackPanel形成鲜明的对比）
也有Orientation属性。
可以直接使用Panel，不用Grid。

L13：
DockPanel





